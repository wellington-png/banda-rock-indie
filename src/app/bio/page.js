import { AboutView } from 'src/sections/bio/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'About us',
};

export default function AboutPage() {
  return <AboutView />;
}
